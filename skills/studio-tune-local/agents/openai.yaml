interface:
  display_name: "Studio Tune Local"
  short_description: "Tune Post Studio safely in the local checkout"
  default_prompt: "Use $studio-tune-local to implement and verify this Post Studio update in my local checkout."
