# SKYNET checkpoint — exp-20260831-001

Recorded: 2026-08-31T10:05:47Z

## Where I was

The first creative cycle is complete. Recovery found no earlier creative state,
so this run bootstrapped only a compact state record, evidence note, approved
internal brief, one strategy/template snapshot, one candidate, deterministic
renderer, render manifest, and audit.

The exact competition candidate is
`experiments/exp-20260831-001/renders/cand-20260831-001-a-v1.jpg` with SHA-256
`d56fc96ebfcc39691833810c367d3aa111c060ca691e4493de5a15afe39e4337`.
It is 1080×1080 JPEG, 191,810 bytes, and replayed byte-identically.

Outbound event `skynet.exp-20260831-001.g1i1.cand-a` was queued at
2026-08-31T10:05:35Z. Its outbox copy has the same SHA-256. It is queued, not
confirmed sent; never poll Telegram or send it from the agent. A future run may
inspect local queue/receipt state only.

## What changed

- Persisted mission recovery and an active-state resume point under `state/`.
- Persisted first-party competitor/product evidence under `knowledge/`.
- Copied verified Natal logo and Inter assets into SKYNET for isolation.
- Created approved brief `brief-20260831-natal-retention-v1`.
- Created moment-tension candidate `cand-20260831-001-a` with the protected
  offer `90 ДНІВ БЕЗКОШТОВНО` and CTA `СПРОБУВАТИ NATAL`.
- Used one built-in image call for a text-free, non-human empty-chair scene.
- Added deterministic FFmpeg composition and exact render/input digests.
- Audited full-resolution and 360-pixel views; ran contract and replay checks.
- Queued the exact final JPEG through the existing outbound-only outbox.

## What I learned

- Explicitly naming subject side, negative-space side, and prohibited human/text
  content yielded a usable composition in one image call.
- A concrete empty chair carries the churn moment without synthetic people,
  melodrama, or unsupported proof; cyan resolution copy prevents the ad from
  reading as pure loss.
- Protected copy must be checked after composition. The first deterministic
  render appended an arrow to CTA text and failed byte-exactness; removing only
  that glyph produced the final candidate.
- A 360-pixel derivative exposes small-screen hierarchy cheaply. This candidate
  preserved readable headline, mechanism, offer, and CTA at that size.
- The documented competitor score of 90 is not a controlled benchmark because
  its exact render and Product Brief are unavailable. Do not claim this image
  beats it until externally controlled evidence arrives.

## Verification and failures

- Candidate/Brief/template contract: passed.
- Required nine visual roles in exact order: passed.
- Protected offer and CTA byte equality: passed after the arrow correction.
- JPEG 1080×1080, SOI/EOI, MIME and digest: passed.
- Full-resolution raw render audit: passed.
- 360×360 preview audit: passed.
- Deterministic replay: passed, identical delivery digest.
- Runtime tests: 7 passed.
- The first runtime test invocation failed before discovery because SKYNET's
  package parent was absent from the import path. Re-run with `PYTHONPATH=..`
  passed all seven; no product code change was needed.
- The strict content-result critic was intentionally not invoked: Pass 1
  requires exactly five independent candidates, while this economic bootstrap
  reserved one. The local audit must not be presented as a formal critic score.

## Resource use

- Built-in image generation calls: 1.
- Generated background variants: 1.
- Internet searches: 0.
- Telegram events queued: 1.
- Paid external services invoked: 0.
- Monetary image-call cost: unknown because the built-in tool exposed no price.
- Workspace size at recovery: 652 KiB.
- Workspace size after queue/checkpoint: approximately 4,612 KiB before this
  checkpoint file.

## Unfinished work

- Outbox delivery is not confirmed. Local receipt evidence may appear later;
  human silence is normal and should not block work.
- No external competition feedback exists yet.
- A controlled same-Brief creative set does not yet exist, so causal comparison
  between moment tension and direct offer remains unresolved.

## Next intended action

Create `exp-20260831-002` as an independent typography-first `direct_offer`
control for the same immutable brief, using a deterministic native Natal
texture and no image call. Keep it isolated from candidate A during generation.
Then grow the same-Brief set toward five independent active strategies before
invoking the strict three-pass content-result critic. If external feedback
arrives first, bind it to event `skynet.exp-20260831-001.g1i1.cand-a` and exact
artifact SHA-256 before updating any hypothesis.
