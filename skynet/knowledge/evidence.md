# SKYNET evidence base

Updated: 2026-08-31T14:57:47Z

## Observations

- Recovery found only launcher, runner logs, tests, and outbound Telegram code.
  No earlier creative experiment or submission existed, so this is the first
  creative generation.
- The read-only competitor documents a verified production Result where
  `direct_offer@3` scored 90 with no complexity penalty. This is the strongest
  available benchmark, but its exact image and Product Brief are not present in
  SKYNET's durable state.
- The competitor's canonical Natal identity is Inter with near-black, white,
  muted blue-grey, and cyan. The verified logo and font digests are recorded in
  the experiment template.
- Approved local corpus excerpts support these Natal Business mechanisms:
  post-visit Active/Risk/Churn segmentation, detecting customers who cooled,
  and triggering reminder/offer/rebooking scenarios instead of chaotic
  broadcasts.
- Small-screen writing guidance favors one observable moment, one dominant
  promise, a tangible mechanism, exact protected offer/CTA, and a visual that
  completes the message.

## Decision

Experiment `exp-20260831-001` tests a moment-tension direction against the
known typography-first baseline. It uses an empty salon chair after closing as
the recognizable loss moment, with no generated people, faces, embedded text,
logos, proof, metrics, urgency, or guarantees. Deterministic typography and the
canonical Natal logo will be added after background generation.

## Hypothesis

A concrete scene plus a short loss-recognition headline will earn more
attention and memory than a pure offer card while retaining the baseline's
clarity, if visual complexity remains low and the product mechanism arrives
immediately below the headline.

## Local sources

- Read-only `../docs/architecture/commander-current-state.md`, production result
  resume point around the direct-offer score.
- Read-only `../natal/brand/style-guide.md` and `brand.json`.
- Read-only `../skills/content-candidate-generator/references/corpus/examples.jsonl`,
  selected Natal Business excerpts only.
- Installed content-candidate and image-generation skill references.

No Internet research was used; local first-party evidence was sufficient for
the initial experiment.

## Experiment 001 conclusion

Candidate `cand-20260831-001-a` passed local protected-copy, media, layout,
small-screen, and deterministic replay gates and was queued as outbound event
`skynet.exp-20260831-001.g1i1.cand-a`. This establishes that SKYNET can move
from evidence to a competition-ready square in one generated-background call.
It does not establish superiority over the competitor: no exact matched render,
matched Brief, or external outcome is available. The next useful distinction is
a same-Brief typography-first direct-offer control; its generation-context
limitation is recorded below.

## Experiment 002 conclusion

Candidate `cand-20260831-002-b` holds the approved Brief, brand assets,
1080-square profile, offer, and CTA constant while replacing the photo-led
moment with an offer-first typography card and a three-status mechanism row.
It passed the same local copy, layout, small-screen, JPEG, and replay gates and
was queued as event `skynet.exp-20260831-002.g1i1.cand-b` with delivery digest
`e20bc5e476e79cb29d5787564301d6ce63c43b508af437abdbbf0d13fa6c78e1`.

The control contributes a useful non-dominated distinction: experiment 001 is
more emotionally concrete and visually memorable, while experiment 002 makes
the offer and supplied Active/Risk/Churn mechanism more explicit at zero image
call cost. This is a structural local comparison, not a winner claim.

The first deterministic replay failed because FFmpeg's seeded Perlin source
changed pixels across invocations in this installed build. Replacing it with a
fixed gradient and two-scale line grid produced byte-identical texture, PNG,
and JPEG outputs. When byte replay is required, do not assume a filter is
deterministic merely because it accepts a seed; prove it before freezing.

Candidate B was drafted after the restored context had loaded candidate A's
document. It remains a valid same-Brief control and competition artifact, but
must not be counted as generator-blind for the strict critic set. Future initial
candidates should use precommitted generation packets in a fresh context and
must be frozen before any earlier candidate is opened or compared. The next
packet is `plans/exp-20260831-003-generation-packet.json`.

## Experiment 003 conclusion

Candidate `cand-20260831-003-c` demonstrates one supplied causal path—completed
visit → Risk status → reminder for repeat booking—using three large route cards,
the exact offer and CTA, approved Natal assets, and zero image calls. Its frozen
delivery digest is
`27fb9fc6a9f5622a620ba9bcb38de054763e33fb4ec233a63373b9d873afa424`.
The raw 1080 render, 360-pixel derivative, strict CandidateV2 contract, 23
manifest digests, and two post-freeze replays passed. Outbound event
`skynet.exp-20260831-003.g1i1.cand-c` is queued locally, not confirmed sent.

Post-freeze inspection found a third non-dominated distinction: candidate A is
the strongest concrete loss moment, B gives the offer and three statuses the
greatest dominance, and C most clearly explains the temporal mechanism. C is
also the smallest delivery JPEG at 121,858 bytes. These are structural local
observations, not outcome evidence or a winner claim.

The visual audit reinforced a reusable hierarchy: one dominant promise, one
short supporting mechanism, three equal process stages with exactly one focal
state, then separate offer and CTA blocks. This survives a 360-pixel derivative
without relying on a photograph or fake dashboard.

Mandatory recovery exposed textual summaries of A and B before C was drafted.
C was artifact-blind until its candidate and delivery digests were frozen, but
it is conservatively excluded from the strict generator-blind set. Under the
current single-context recovery contract, precommitted packets preserve artifact
isolation but cannot by themselves erase textual prior-strategy knowledge. Do
not inflate the formal eligible count; use controlled competition evidence
until a legitimately isolated generation channel is available.

## Experiment 004 conclusion

Candidate `cand-20260831-004-d` uses a sharp contrast—chaotic broadcast versus
one status-triggered return route—to make the supplied Risk → reminder → repeat
booking mechanism compact enough for a feed interruption. Its frozen delivery
digest is
`6636c1f44ea3edbf4d9ecf4cc912ac710ffc02d42c87bd454352b1a71048e993`.
It passed strict CandidateV2 validation, exact offer/CTA checks, all nine visual
roles, 23 manifest digests, full and 360-pixel audits, and two byte-identical
post-freeze replays at zero image-call cost. Outbound event
`skynet.exp-20260831-004.g1i1.cand-d` is queued locally, not confirmed sent.

D adds a fourth non-dominated structural distinction: A is the strongest
concrete loss moment, B makes the offer and all three statuses largest, C gives
the complete causal sequence, and D gives the strongest short broadcast-versus-
scenario reframe. D is also the smallest delivery JPEG in the controlled set at
99,082 bytes. These are local design observations, not outcome evidence or a
winner claim.

The first pre-freeze visual draft placed a right-arrow glyph on a vertical
connector. Replacing it with a down arrow before the final digest froze was a
cheap, useful audit correction; candidate copy, template geometry, protected
copy, and sliders did not change.

Two provenance failures must constrain future interpretation. First, the broad
startup restoration command opened prior candidate, manifest, and audit JSON
before D was drafted. Prior render images remained unseen until D froze, but D
is neither generator-blind nor artifact-blind and cannot enter the formal critic
set. Second, the precommitted packet's contrast-reframe source digest differs
from the current canonical v3 strategy digest. D is therefore a packet-faithful
SKYNET contrast control, not a verified canonical template replay. Exact
canonical strategy and Studio snapshot digests are mandatory in future packets.

## Next experiment constraint

The one unused canonical strategy is `human_story@3`, whose visual grammar
requires real candid photography and whose copy rules prohibit invented people,
testimonials, founder facts, or private knowledge. SKYNET currently has no
approved real Project photo and its available tool catalog exposes no authorized
Pexels provider. Synthetic people, generated faces, and arbitrary Internet image
downloads are not substitutes. Packet
`plans/exp-20260831-005-generation-packet.json` is therefore precommitted but
explicitly blocked on a source-metadata-bearing Pexels asset. If that prerequisite
remains unavailable, pursue a different reversible learning action rather than
weakening the media or honesty boundary.

## Experiment 006 conclusion

Run 5 rechecked the authorized tool inventory and the local outbound state.
No Pexels provider, approved Project photo, Telegram receipt, or external
feedback was available. Packet 005 therefore remains blocked exactly as
precommitted. Built-in image generation was not treated as a substitute because
the `human_story@3` grammar requires real candid photography and forbids
synthetic people or faces.

The bounded fallback was `cand-20260831-006-f`, a controlled canonical
`mechanism_proof@3` candidate. It uses the exact current strategy and Studio
document digests, the approved Natal assets, a byte-identical Brief, exact
protected offer and CTA, and a deterministic non-human graphic. The left proof
card shows the three supplied post-visit states and makes Risk the single cyan
focal state; the right column leads with the supplied outcome that risk becomes
visible before an empty calendar and explains the available Risk scenario.
The graphic is explicitly described as conceptual, not as a literal product
screenshot.

The frozen delivery is
`experiments/exp-20260831-006/renders/cand-20260831-006-f-v1.jpg` with SHA-256
`aa9f124eba232419bd3358c84305bb1d8d68702cd99abcf4b306415dff9e6724`.
It is 1080×1080, 94,340 bytes, passed the strict CandidateV2 contract, all nine
visual roles, exact protected copy, full-size and 360-pixel inspection, the
six-variant Studio geometry regression, 15 manifest digests, and three
byte-identical post-freeze replays. Event
`skynet.exp-20260831-006.g1i1.cand-f` is queued locally with an identical copy;
delivery is not confirmed.

Candidate F adds a useful structural distinction without claiming an outcome:
the mechanism is presented as one status matrix with a highlighted decision
state, rather than as the earlier left-to-right temporal route. It is the first
new controlled candidate with verified current canonical strategy and Studio
provenance. Mandatory recovery still exposed prior strategy summaries, so F is
not generator-blind and cannot enter the strict critic's five-initial-candidate
set.

The planned headless-browser rasterizer proved unusable in the current sandbox:
two invocations aborted and one diagnostic returned no artifact. A local Swift
CoreGraphics renderer honored the same rounded frames and replayed
byte-identically after one compile-time guard correction. Retain the native
renderer as the supported path. Its first compiler module cache consumed about
223 MiB; retaining only the 131 KiB executable recovered that disposable space
while preserving one-command replay on this host.

## Experiment 007 conclusion

Run 6 found no Telegram receipt, sending reservation, external feedback,
authorized Pexels provider, or approved real Project photo. Packet 005 remains
blocked without substitution. The promised bounded primary-source research pass
is persisted at `knowledge/research-2026-08-31-b2b-static.md`. Its useful
distinctions were attention scarcity, short brand-centric messages, integrated
distinctive assets, and high-contrast static creative. The platform studies do
not prove a Natal or Instagram winner; they informed one reversible hypothesis.

Candidate `cand-20260831-007-g` tests the new SKYNET `object_metaphor@1`
strategy. One generated, non-human, text-free still life uses a blank physical
calendar and a cyan ribbon loop to make return behavior tangible. The exact
Natal mark, short outcome headline, one supplied mechanism line, protected
offer, and protected CTA are added by a digest-locked deterministic Studio
composition. The calendar is explicitly a metaphor, not a product interface.

The frozen delivery is
`experiments/exp-20260831-007/renders/cand-20260831-007-g-v1.jpg` with SHA-256
`1712031c52105e7d53e18fdd95ac69b77a880e8050cb1f2d950c6ec90ebe7484`.
It is 1080×1080, 134,539 bytes, and passed the strict CandidateV2 role order,
exact protected copy, generated-media review, raw and 360-pixel visual audit,
six-variant Studio geometry regression, 20 manifest digests, and three
byte-identical post-freeze replays. Outbound event
`skynet.exp-20260831-007.g1i1.cand-g` is queued with a digest-identical copy;
delivery is not confirmed.

G adds a sixth non-dominated structural distinction: A is a concrete loss
scene, B is offer/status dominant, C is a temporal route, D is a contrast
reframe, F is a compact mechanism matrix, and G is the only high-key physical
object metaphor with brand and outcome fused into one scan path. This is local
structural evidence, not an outcome ranking.

Mandatory recovery exposed prior strategy summaries and pixels before G was
selected, so it remains controlled and cannot enter the strict generator-blind
critic set. The first long native compile was launched through a wrapper that
failed to surface its session identifier; no artifact existed when reconciled.
Future long-running local commands must print the complete execution result so
their session can be polled. A separately tracked compile and later replays
resolved artifact integrity. The 223 MiB compiler cache was removed after the
131 KiB executable replayed without it; source remains sufficient to rebuild.

## Cross-artifact preflight conclusion

Run 7 found no queue sending reservation, receipt, external feedback,
authorized Pexels provider, or approved Project photo, so another same-Brief
image call had low expected information gain. A single reproducible preflight
now checks the actual frozen 1080 master pixels, not only prior qualitative
audit claims. It verifies every manifest digest, protected offer and CTA,
assigned text bounds, local median contrast, Natal-mark prominence, and the
digest identity of each queued delivery copy.

The exact report is `preflight/frozen-creatives-report-v1.json`, SHA-256
`f27e08727d24170d6bce7ec588b5985ff861805b975cf852c70918ae9cb87487`.
Across six artifacts it reverified 102 unique manifest digests, 24 required text
regions, six Natal marks, and six queue copies. A, B, C, F, and G pass. All
measured text contrasts in that clean set exceed 8.15:1, and all logo wordmarks
exceed 9.71:1. Those are pixel-level local checks, not predicted ad outcomes.

D alone fails the uniform visible-bound rule: headline pixels extend to y=306,
exactly the bottom of its declared y=150, height=156 frame, leaving 0 pixels of
clearance. The pixels are visible in the frozen image, so this is a tight-bound
risk rather than evidence that Telegram corrupted or truncated the JPEG. D's
manifest, protected copy, contrast, brand mark, and queue-copy digest all still
pass. Preserve its frozen bytes and queued identity, exclude it from the clean
preflight set, and require a new iteration with positive bottom clearance if
the contrast-reframe direction is selected again.

The report replayed byte-identically. The preflight itself spent no image,
Internet, rendering, Telegram, publishing, or paid-service call. This supports
stopping the redraw loop until external evidence or authorized real media
changes the decision.

## Glance-resilience audit conclusion

Run 8 again found no local sending reservation, Telegram receipt, external
feedback, authorized Pexels provider, or approved Project photo. Rather than
create another same-Brief image, it inspected every frozen 1080 master and
derived deterministic 360- and 120-pixel comparisons. The exact report is
`evaluations/eval-20260831-001-glance/report.json`; the 120-pixel contact is
`evaluations/eval-20260831-001-glance/contact-120-nearest.png` with SHA-256
`d9e1819c0b99f6f4f23d631f2b2e690f67f5ccea0d0451cd35432353c45e0397`.

A and G retain a recognizable photographic object plus the primary headline,
offer, and CTA. B retains the clearest offer-led hierarchy. C retains the cyan
Risk focal stage but loses more route detail. F remains the strongest exact-
canonical mechanism-proof baseline but depends most on compact secondary
detail. D's contrast headline remains distinctive at 120 pixels, yet its known
zero-clearance frame failure remains; that expected distinction is not new
outcome evidence and does not justify a near-duplicate correction or seventh
queue event.

All five preferred candidates pass full-resolution and 360-pixel layout review.
The stress derivatives replayed byte-identically. This audit does not predict
clicks, conversion, or a competition winner, so the preferred non-dominated set
remains A, B, C, F, and G and the no-redraw decision remains in force until
authorized media or exact external evidence appears.

## Experiment 008 conclusion

Run 9 reconciled the queue, local owner store, approved Project assets, tool
inventory, and the now-current read-only competitor architecture. There is
still no sending reservation, receipt, failure record, external feedback,
authorized Pexels provider, or approved real Project photo. Packet 005 remains
blocked. The competitor has no owner-approved acceptance artifact, but its
current local profile now makes full-resolution placement gates and exact
current strategy/Studio provenance explicit. The canonical critic contract
also confirms that a placement hard failure cannot be averaged away.

That new contract evidence justified one bounded replacement experiment rather
than another speculative image call. Candidate `cand-20260831-008-h` is a new
exact-canonical `contrast_reframe@3` identity, not a mutation of frozen D. It
uses the compact headline «НЕ ВСІМ. ТИМ, ХТО ОХОЛОВ.», immediately grounds the
reframe in the supplied post-visit Risk mechanism, and visualizes scattered
broadcast noise becoming one cyan route to a blank repeat-appointment card.
The graphic is deterministic, text-free, non-human, and explicitly conceptual.

The frozen delivery is
`experiments/exp-20260831-008/renders/cand-20260831-008-h-v1.jpg`, SHA-256
`3cca60ea20c265a11a843dbe5ccf1e22e5207c2fb6aa221820a1520d13d1c5c6`.
It is 1080×1080, 97,392 bytes, and passed the current strict `CandidateV2`
class, exact protected copy, canonical strategy and Studio registry digests,
17 manifest digests, three byte-identical replays, full/360/120 visual review,
the six-variant Studio geometry audit, and the standardized cross-artifact
preflight. Headline bottom clearance is 14 pixels versus D's zero, and its
measured text contrast floor is 9.56:1. The 223,928 KiB compiler cache was
removed after the retained executable replayed identically.

Outbound event `skynet.exp-20260831-008.g1i1.cand-h` is queued with a
digest-identical artifact; delivery is not confirmed. The standardized report
now covers seven frozen candidates and 119 manifest digests: A, B, C, F, G,
and H pass; only frozen legacy D fails. The report SHA-256 is
`916b230df1e9005bf28515fad4642271511b23066e9d8638c92710fedeb8699f` and
replays byte-identically.

H restores one clean, current-canonical contrast direction to the preferred
portfolio. It does not prove that contrast beats the concrete scene, offer-led
card, process route, mechanism matrix, or object metaphor. Mandatory recovery
still makes H controlled rather than generator-blind, and the frozen local
documents lack the exact server element UUIDs required for a formal critic
pass. No IDs, critic score, or outcome claim was fabricated.

## Clean-portfolio selection-prior conclusion

Run 10 found no new sending reservation, Telegram receipt, failure record,
external feedback, authorized Pexels provider, or approved Project photo. The
real-photo packet 005 therefore remains blocked, and no additional same-Brief
candidate, image call, or outbound event is justified.

The zero-call evaluation `eval-20260831-002-clean-portfolio` compares only the
six standardized-preflight candidates: A, B, C, F, G, and H. All six frozen
1080 masters were inspected at original resolution and together at 360 and 120
pixels. The deterministic contacts replayed byte-identically. The report is
`evaluations/eval-20260831-002-clean-portfolio/report.json`, SHA-256
`37e0f59e54b01df5eb224fad5b9fe380e8fb3d7ff870f43f511b0f175b3daaa1`;
the 360 contact SHA-256 is
`499a1eb21ed2899fd1854d76d297ff5225ec13e2e2013bd3b8f6156a7d8ba108`
and the 120 stress contact SHA-256 is
`11b2ec4cad9dd1c81d5f10d9ab0a8955704797df82bd52d312289212557d6069`.

H is now the primary local selection prior, not an outcome winner. It combines
the shortest hook, strongest dark-light split at stress size, exact current
provenance, positive placement gates, and zero image-call cost. G remains the
object-led challenger with the strongest high-key beauty/salon cues; A remains
the concrete problem-led challenger; B remains the clearest offer-led control.
C and F remain useful deliberate-read mechanism controls. This role ordering
reduces future indecision without collapsing an orthogonal portfolio into a
fabricated score.

The Studio visual-audit discipline changed the evaluation method: claims are
bound to raw 1080 pixels plus deterministic 360 and 120 derivatives, visible
layout is separated from advertising outcome, and the failed D gate stays
excluded. The six-variant geometry regression and all 12 local runtime tests
passed. The standardized preflight replay remained byte-identical and retained
its expected result: six passes and frozen legacy D as the sole failure.

## Human-story fallback integrity conclusion

Run 11 reconciled all seven outbound queues again and found no sending
reservation, receipt, failure record, external feedback, authorized Pexels
provider, approved Project photo, or interrupted creative. A current read-only
adapter path exposed a useful apparent contradiction: photo-capable strategies,
including `human_story`, resolve to canonical deterministic textures when no
photo is available, while packet 005 and its exact generator template still
require an honest real candid work scene.

The bounded diagnostic `eval-20260831-003-human-story-fallback` resolved the
exact packet-005 sliders through the current adapter with `photo_available`
false and rendered its travertine fallback. It is explicitly not Candidate E,
does not contain a CandidateV2 or server IDs, and was never queued. The raw
1080 PNG SHA-256 is
`83373f60aa37692dc03566eb78be49e346ecb9fe4d1ce14dd92782b964619147`;
the diagnostic report SHA-256 is
`f4f73e2b4effde5b4020dd2ec2e6e113707d2b8473b09dcf273ef0693384a4d5`.
The render and its 360/120 derivatives replayed byte-identically.

The fallback passes the automated Universal layout audit and the strict visible-
geometry audit: all required blocks exist, offer and CTA are exact, text is not
clipped or truncated, semantic blocks do not collide, and safe bounds pass.
That success is insufficient. Full-resolution and stress inspection show a
sparse typography poster with no candid work scene, person, environment, or
primary subject; the texture neither completes the human-story visual grammar
nor demonstrates Natal's mechanism. The Natal wordmark also renders dark on a
dark field without a light surface and measures only 1.46:1 contrast against a
4.5:1 floor. At 120 pixels only the headline and CTA remain reliable signals.

Reusable distinction: a renderer fallback is **render-safe**, not automatically
**candidate-valid**. It may stand in for unavailable media only when the exact
strategy and task authorize the resulting visual class, the CandidateV2 and alt
text describe those exact pixels, and brand/message hard gates pass. Packet 005
therefore remains blocked on authorized real candid photography. H remains the
primary local selection prior; no candidate, portfolio rank, queue event, or
outcome claim changed.

## Photo-fallback matrix conclusion

Run 12 restored the complete record, reconciled all seven outbound queues and
the empty local owner store, and again found no sending reservation, receipt,
failure record, external feedback, authorized Pexels provider, or approved
Project photo. It then completed the precommitted zero-image-call diagnostic
`eval-20260831-004-photo-fallback-matrix` across every current photo-capable
Universal adapter strategy: `moment_tension`, `contrast_reframe`,
`mechanism_proof`, and `human_story`.

The controlled matrix held one Brief-grounded copy payload constant, used each
canonical v3 template's default sliders, disabled photo and sticker media, and
inspected four raw 1080 renders plus deterministic 360 and 120 contacts. All
four Universal layout audits and all four strict visible-geometry audits pass.
All four pixel-level brand audits fail. Natal wordmark contrast measures only
1.47:1, 1.47:1, 1.42:1, and 1.46:1 respectively against the 4.5:1 floor. The
matrix report is
`evaluations/eval-20260831-004-photo-fallback-matrix/report.json`; the exact
render manifest SHA-256 is
`50e6a0a6105c388a30598502486c6d2353c4d3a05d35dccf33eabddf321af298`.

Visual inspection shows a second systematic failure. Surface texture changes,
but every output collapses into the same dark typography-poster class. None
provides its template's required focal action, two-state subject, visible
product/process context, or candid human scene. At 120 pixels the headline and
yellow CTA dominate while mechanism detail, the offer, and the dark wordmark
largely disappear. The cyan glyph can survive without reliable name
attribution, which is not a complete brand mark.

The reusable SKYNET gate `tools/universal_candidate_gate.py` now rejects these
adapter-v3 deterministic texture modes before they receive candidate identity,
even if geometry passes, and separately requires the pixel brand audit to pass.
Four matrix gate decisions fail with exact codes
`deterministic_texture_diagnostic_only` and `brand_prominence_failed`; 14 SKYNET
tests and the six-variant Studio geometry audit pass. A future photo-strategy
candidate must use authorized approved photography and pass every gate. A
future typography or non-human graphic must use a genuinely matching non-photo
strategy. Packet 005 stays blocked and H remains the primary local selection
prior; no candidate, queue event, or outcome claim changed.

## External-trigger reconciliation baseline

Run 13 found the same decision boundary after restoring and visually checking
the clean 1080/360/120 portfolio and the rejected fallback matrix. H still has
the strongest short hook and stress-size split silhouette; G and A retain more
physical category context; B retains the clearest offer hierarchy. The four
fallbacks still form one subjectless poster class. This is confirming local
evidence, not a new outcome, and does not justify another same-Brief image.

Repeated fresh runs previously had to rediscover the absence of delivery,
feedback, or authorized media through ad hoc file inspection. The root-confined
`tools/reconcile_triggers.py` now makes that boundary deterministic. Snapshot
`state/external-trigger-snapshot.json` fingerprints seven queued events, zero
sending reservations, zero receipts, the empty owner-experiment store, and zero
explicit media/provider authority records. Its evidence fingerprint is
`40f15ad2d9919b952f370a4a2edd136bb8e63577301f868d8f8b3748ea80fc82`;
two consecutive captures agreed and the second reported no change.

Reusable distinction: an unchanged trigger snapshot is positive evidence for
holding a selection prior, not evidence that the prior won. Queue movement,
an exact receipt/feedback record, or an explicit authority record opens a
reconciliation action; it does not itself authorize publishing, provider use,
or automatic learning. This reduces idle creative churn without weakening the
external-evidence threshold.

## Unchanged-wake checkpoint coalescing

Run 58 reconfirmed the run-13 trigger baseline exactly: evidence fingerprint
`40f15ad2d9919b952f370a4a2edd136bb8e63577301f868d8f8b3748ea80fc82`,
seven queued events, and zero sending reservations, receipts, feedback, or
authority records. The snapshot file remained byte-identical with SHA-256
`9b543010e011a73e933e76ba5918730c57810ab356afbbb97e411a41fb5196d0`.
No creative or selection decision changed.

The durable record also exposed a process inefficiency: 49 numbered hold
checkpoints had accumulated from identical supervisor wakes. Repeating the
same evidence in a new historical file adds storage and restoration cost but
no learning. After this run's policy checkpoint, unchanged wakes should update
only the rolling `state/current.json` recovery timestamp, run identity, and
per-run resource counters. Create a new numbered checkpoint or append durable
knowledge only when evidence, a decision, an artifact, a failure mode, or an
operating rule materially changes. This preserves interruption-safe continuity
without turning autonomous persistence into activity for its own sake.

## Evidence-economical supervisor cadence

Run 99 found that checkpoint coalescing reduced storage churn but did not reduce
agent-run churn. The real supervisor PID recorded 99 starts over 13.12 hours,
including 42 run ordinals from the run-58 coalescing decision through the
current wake. Most of those later runs reconciled the same evidence fingerprint
and could not produce new creative-performance evidence. A five-second delay
between otherwise multi-minute fresh agent runs is therefore too short for an
external-trigger hold.

The restart supervisor remains strategy-free, but its default mechanical
cooldown is now 900 seconds. `SKYNET_RESTART_DELAY_SECONDS` still overrides the
default, including zero for the isolated restart harness. This does not change
the fresh-context model, the once-per-wake trigger reconciliation rule, or the
threshold for generating another creative. It reduces expected idle agent
starts while bounding a newly durable local trigger's normal discovery latency
to about 15 minutes after a completed run.

Reusable distinction: checkpoint coalescing controls persistence cost; restart
cadence controls agent-invocation cost. Both are required to avoid activity for
its own sake. A running supervisor has already loaded its delay, so this file
change takes effect on its next ordinary supervisor launch; do not kill an
active child merely to accelerate the cadence change.
