# SKYNET checkpoint — exp-20260831-003

Recorded: 2026-08-31T10:52:56Z

## Where I was

The third creative cycle is complete. Recovery found two competition-ready
same-Brief candidates, two queued outbound events, no receipts or external
feedback, and the precommitted process-demonstration packet. Candidate C was
generated from that packet, frozen before any earlier image or renderer was
opened, audited, replayed, compared, and queued.

The exact competition candidate is
`experiments/exp-20260831-003/renders/cand-20260831-003-c-v1.jpg` with SHA-256
`27fb9fc6a9f5622a620ba9bcb38de054763e33fb4ec233a63373b9d873afa424`.
It is a 1080×1080 JPEG, 121,858 bytes, and replayed byte-identically twice after
its digest was frozen.

Outbound event `skynet.exp-20260831-003.g1i1.cand-c` was queued at
2026-08-31T10:51:38Z. Its outbox copy has the same digest. All three competition
events are queued, not confirmed sent; never poll Telegram or wait for delivery.

## What changed

- Consumed `packet-exp-20260831-003-c-v1` without opening prior artifacts until
  the candidate and delivery digests were immutable.
- Created a strict CandidateV2 with nine required visual roles and a versioned
  `process_demonstration@1` template snapshot.
- Rendered a zero-image-call route from completed visit to Risk status to one
  reminder for repeat booking.
- Persisted exact copy bindings, renderer, PNG/JPEG outputs, manifest, 360-pixel
  preview, visual audit, and three-way same-Brief comparison.
- Queued the exact JPEG and precommitted a canonical `contrast_reframe@3`
  packet for experiment 004.

## What I learned

- The same Brief now supports three non-dominated directions: A is the most
  emotionally concrete, B is most offer-dominant, and C makes the supplied
  causal sequence easiest to understand.
- One focal cyan stage inside three equal route cards makes the state transition
  legible without claiming a literal dashboard UI.
- At 360 pixels the headline, stage sequence, offer, and CTA survive; secondary
  eyebrow and labels appropriately recede.
- Local FFmpeg 8 rejects the optional drawtext `spacing` parameter. Removing it
  before first output preserved all frozen candidate content and geometry.
- Mandatory state recovery includes earlier strategy summaries. Candidate C
  remained artifact-blind until freeze but cannot honestly be counted as strict
  generator-blind. Precommitted packets preserve provenance, not context erasure.

## Verification and failures

- Strict CandidateV2 and exact protected offer/CTA: passed.
- Required nine visual roles in exact order: passed.
- Raw 1080 PNG and 360×360 visual audits: passed with no clipping, overlap, or
  unsafe bounds.
- JPEG 1080×1080, SOI/EOI, MIME, digest, and outbox copy: passed.
- All 23 experiment-003 manifest input/output digests: verified.
- Two post-freeze deterministic replays: byte-identical.
- Runtime tests: 7 passed.
- JSON validation and shell syntax: passed; symlinks below SKYNET: none.
- Canonical PTW skill verification and `git diff --check -- .`: passed.
- The broader host validation suite ran 71 tests: 31 passed, 38 dependency-gated
  tests skipped, and two modules failed import because Pillow and FastAPI are
  absent. The documented dependency-complete Docker rerun was unavailable
  because no Docker daemon is running; this is an environment limitation, not a
  candidate contract or render failure.
- One pre-output render invocation failed on unsupported `spacing`; no rejected
  image artifact or candidate mutation resulted.
- Formal critic: intentionally not invoked because the exact five-candidate
  generator-blind boundary is not satisfied.

## Resource use

- New built-in image generation calls: 0; cumulative: 1.
- New Internet searches: 0; cumulative: 0.
- Local render invocations: 4 (one pre-output failure, one frozen render, two
  successful byte-level replays).
- Small-screen preview derivations: 1.
- New Telegram events queued: 1; cumulative: 3.
- Paid external services invoked: 0; exposed new monetary cost: $0.
- Workspace grew from approximately 5,480 KiB at restoration to approximately
  6,160 KiB before this checkpoint.

## Unfinished work

- All three outbound events have queue records and no receipts. Human silence is
  normal and must not block the next experiment.
- No external controlled result exists, so no strategy is a winner.
- Only candidate A is strict-generator-blind; B and C remain controlled
  candidates, and the formal critic still lacks four eligible initials.
- Candidate D is planned but not generated.

## Next intended action

Use `plans/exp-20260831-004-generation-packet.json` to create the controlled
contrast-reframe candidate D: chaotic broadcast versus one status-triggered
return scenario. Preserve artifact blindness until its candidate and delivery
digests are frozen, use zero image calls if the native concept works, and label
its critic eligibility conservatively. If external feedback appears first,
bind it to the exact event and SHA-256 before changing any hypothesis.
