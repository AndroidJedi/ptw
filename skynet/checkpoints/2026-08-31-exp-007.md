# SKYNET checkpoint — exp-20260831-007

Recorded: 2026-08-31T12:42:57Z

## Where I was

Run 6 restored the complete state through experiment 006: five frozen
competition artifacts and queue events, exact checkpoints and provenance, no
sending reservation, receipt, external feedback, interrupted creative, Pexels
provider, or approved real Project photo. Packet 005 remains blocked because
`human_story@3` requires source-metadata-bearing candid real photography.

The promised bounded fallback is complete. Candidate
`cand-20260831-007-g` is a controlled SKYNET `object_metaphor@1` competition
candidate. Its exact delivery is
`experiments/exp-20260831-007/renders/cand-20260831-007-g-v1.jpg`, SHA-256
`1712031c52105e7d53e18fdd95ac69b77a880e8050cb1f2d950c6ec90ebe7484`.
It is a 1080×1080 JPEG, 134,539 bytes, with three byte-identical post-freeze
replays.

Outbound event `skynet.exp-20260831-007.g1i1.cand-g` was queued at
2026-08-31T12:33:55Z. The outbox artifact has the same digest. All six SKYNET
events remain queued with no receipts; never poll Telegram or wait for delivery.

## What changed

- Persisted one bounded primary-source research pass at
  `knowledge/research-2026-08-31-b2b-static.md`.
- Added digest-locked SKYNET `object_metaphor@1` strategy and
  `object_metaphor_square@1` Studio documents.
- Precommitted packet `packet-exp-20260831-007-g-v1` before media generation.
- Used one built-in image call for one text-free, person-free still life: a
  blank calendar and cyan physical return loop with restrained salon tools.
- Generated one strict CandidateV2 with nine ordered visual roles, exact
  `90 ДНІВ БЕЗКОШТОВНО` offer, and exact `СПРОБУВАТИ NATAL` CTA.
- Composited the approved Natal logo and deterministic copy with Swift
  CoreGraphics; exported PNG, baseline JPEG, and 360-pixel audit derivative.
- Persisted manifest, visual audit, source prompt, copy bindings, renderer,
  candidate, experiment record, and frozen source-media digest.
- Queued the exact frozen JPEG through the outbound-only Telegram outbox.
- Removed approximately 223 MiB of reproducible Swift compiler module/temp
  cache after retaining the 131 KiB renderer and proving it replays without the
  cache. The deleted cache is recoverable from `render.swift`.

## What I learned

- Primary LinkedIn/MAGNA, LinkedIn/VidMob, B2B Institute/MediaScience, and CMO
  Scorecard evidence supports a test—not a winner claim—around short
  brand-centric messages, integrated distinctive assets, high contrast, and a
  visual idea that survives fragmented attention.
- A familiar physical object can express an intangible retention mechanism
  without a fake dashboard. The blank calendar plus one cyan return loop is
  readable before the copy and remains coherent at 360 pixels.
- High-key near-white photography creates a real structural distinction from
  the existing dark diagrams and empty-chair scene. The loop can enter behind
  the final headline line without becoming an overlap defect when visible ink
  retains high contrast and assigned blocks remain separate.
- The one-call image prompt was sufficiently bounded: the output preserved
  clean copy space and contained no people, faces, text, numbers, logos,
  screens, UI, proof, metrics, or watermarks.
- A long-running command wrapper must print the complete execution result, not
  only its output string, or the returned session ID can be lost. The first
  compile produced no artifact at immediate reconciliation; a separately
  tracked compile plus three later replays constrain final integrity.
- Mandatory recovery makes G controlled. Exact SKYNET strategy/Studio
  provenance does not make it generator-blind.

## Verification and failures

- CandidateV2 normalized digest:
  `64dc453a110dc61d078a866f2f1b34c8300937240ca2f912fa572324ecb8e2d8`.
- Strict CandidateV2, exact nine-role order, protected offer/CTA, and non-human
  media policy: passed.
- Generated source review at 1254×1254: passed all no-person, no-text, no-UI,
  no-proof, clean-copy-space gates.
- Raw 1080 PNG and 360×360 visual audits: passed with no clipping, overflow,
  absent block, unsafe bound, or semantic overlap.
- Six-variant Universal Studio geometry regression: passed.
- PNG/JPEG dimensions, RGB MIME, baseline JPEG markers, output digest, and
  queued copy: passed.
- Twenty active manifest input/output digests: verified.
- Three post-freeze delivery/master replays, including one after compiler-cache
  removal: byte-identical.
- SKYNET runtime tests: 7 passed.
- Dependency-complete validation suite: 86 ran, 82 passed, 4 disposable-
  PostgreSQL tests skipped, 0 failures.
- Canonical PTW skill verification, all JSON parsing, all shell syntax,
  symlink scan, and `git diff --check -- .`: passed.
- First local runtime test command had a path-only collection error because
  `PYTHONPATH` omitted the parent package root; corrected invocation ran all
  seven tests successfully. No test body failed.
- First long native compiler invocation lost its polling session ID through the
  wrapper. No artifact was present at reconciliation. The tracked replacement
  compile succeeded; frozen artifact replay is byte-identical.
- Formal critic: intentionally not invoked because only A is generator-blind
  and the strict five-initial-candidate boundary remains unsatisfied.

## Resource use

- New built-in image calls: 1; cumulative: 2.
- New bounded Internet research tool calls: 4; cumulative: 4.
- Local render-script invocations: 5; 4 tracked successes and 1 untracked first
  compile invocation.
- Small-screen preview derivations: 1.
- Post-freeze byte replay invocations: 3.
- New Telegram events queued: 1; cumulative: 6.
- Paid external services invoked: 0; exposed new monetary cost: $0. The built-in
  image call's monetary price was not exposed.
- Workspace size was approximately 9,660 KiB at restoration, peaked near
  236,420 KiB during the disposable compiler cache, and ended near 13,700 KiB
  after cleanup and queueing.

## Unfinished work

- All six outbound events have queue records and no receipts. Human silence is
  normal and does not block useful work.
- No exact external competition result exists, so none of A-G is an outcome
  winner.
- The strict content-result critic still has only A as generator-blind and
  lacks four eligible initial candidates. B, C, D, F, and G remain controlled.
- D remains neither artifact-blind nor current-canonical-template verified.
- F has exact canonical mechanism-proof provenance but remains controlled.
- G has exact SKYNET strategy/Studio provenance but remains controlled.
- E remains blocked until an authorized source-metadata-bearing Pexels photo is
  available. Never substitute generated people, faces, or arbitrary Internet
  media.

## Next intended action

Reconcile local queue, sending, and receipt evidence without polling. If exact
competition feedback appears, bind it to the event ID and artifact digest
before updating any hypothesis. Recheck authorized Pexels/Project media and
activate packet 005 only if its real-photo prerequisite passes. If neither
feedback nor media exists, do not immediately redraw the same Brief again:
implement a low-cost deterministic preflight for visible text bounds, contrast,
and brand prominence across frozen artifacts, then decide whether another image
call offers material information gain.
