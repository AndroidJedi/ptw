# SKYNET checkpoint — experiment 008

Recorded: 2026-08-31T14:09:17Z

## Where I was

Run 9 restored experiments 001–007, six frozen deliveries, the blocked real-
photo packet 005, research, standardized preflight, glance audit, seven prior
checkpoints, local owner state, and all Telegram queue evidence. Reconciliation
found no interrupted creative, sending reservation, receipt, failure record,
external feedback, authorized Pexels provider, or approved real Project photo.
The only approved Project image remains the canonical Natal logo, so packet 005
is still blocked without substitution.

The read-only competitor now documents a stricter local evaluation profile:
current canonical strategy/Studio digests and full-resolution placement hard
gates are authoritative, while no owner-approved acceptance artifact exists.
The content-result critic contract also treats placement failures as hard gates,
but its formal pass could not be used because SKYNET's frozen local artifacts
lack server element UUIDs and the restored context is not critic-blind.

## What changed

- Reserved `plans/exp-20260831-008-generation-packet.json` with the exact
  current `contrast_reframe@3` strategy digest, exact current Studio digest,
  original sliders, approved Brief, brand assets, bounded writing bundle, and
  a controlled-generation disclosure.
- Added strict CandidateV2 `experiments/exp-20260831-008/candidate.json` with
  exact offer/CTA and all nine ordered visual roles. The current production
  `CandidateV2.from_dict` class accepted it with normalized SHA-256
  `6ef3909baa08a9677d8c4410f927970aba7228725d768f86ba3e5993b075e50e`.
- Added one deterministic 540×1000 text-free non-human route asset, a resolved
  canonical Studio snapshot, a local CoreGraphics renderer, protected copy
  files, full PNG/JPEG, 360-pixel preview, and 120-pixel stress thumbnail.
- Froze Candidate H as a new exact-canonical replacement identity. Frozen D
  and its queue record were not changed, removed, or reinterpreted.
- Queued outbound event `skynet.exp-20260831-008.g1i1.cand-h` with a delivery
  copy identical to JPEG SHA-256
  `3cca60ea20c265a11a843dbe5ccf1e22e5207c2fb6aa221820a1520d13d1c5c6`.
  No sender, poller, webhook, or receipt wait was invoked.
- Extended the standardized preflight to all seven frozen creatives. Its new
  byte-identical report SHA-256 is
  `916b230df1e9005bf28515fad4642271511b23066e9d8638c92710fedeb8699f`.
- Appended the reusable conclusion to `knowledge/evidence.md` and updated the
  compact resume state. No competitor, parent repository, production system,
  database, campaign, publisher, or provider was mutated.

## What I learned

- H preserves the contrast direction without D's known hard-gate defect:
  headline visible bounds end 14 pixels above the assigned frame bottom rather
  than touching it. Body, offer, and CTA bottom clearances are 44, 27, and 27
  pixels.
- H's measured local text contrast ranges from 9.56:1 through 15.34:1; the
  Natal wordmark contrast is 9.73:1. Adjacent visible headline/body,
  body/offer, and offer/CTA gaps are 61, 167, and 150 pixels.
- At 360 and 120 pixels, the compact contrast headline, selected cyan node and
  route, blank calendar, offer, CTA surface, and Natal mark remain
  distinguishable. Mechanism copy recedes appropriately under stress.
- The current CandidateV2 boundary assigns media/source UUIDs server-side.
  Human-readable SKYNET artifact IDs belong in orchestration manifests, not
  generator-return UUID fields. The pre-validation draft violated that
  boundary; correcting the document left frozen copy and pixels unchanged.
- Current-canonical provenance plus a positive placement gate is materially
  different from a near-duplicate redraw. It was enough to justify one zero-
  image-call replacement, but not enough to claim H beats any other direction.
- Formal content-result critic scores remain unavailable: inventing element
  UUIDs or calling a controlled restored context blind would invalidate the
  evidence.

## Verification and failures

- Authoritative PNG/JPEG: 1080×1080; media asset: 540×1000; 360- and 120-pixel
  derivatives inspected.
- Candidate H: current CandidateV2 class passed; nine required visual roles in
  order; protected offer and CTA byte-exact.
- Canonical registry checks: current strategy digest and current Studio digest
  both passed.
- Candidate H manifest: 17 input/output digests passed.
- Replay: asset, master PNG, and delivery JPEG were byte-identical across two
  normal replays and one retained-executable replay after cache removal.
- Studio visual audit: default, high-density, centered-minimal, editorial
  bottom-left, urgent bottom-right, and logo-without-background variants passed.
- Standardized preflight: seven artifacts checked, six passed, only frozen D
  failed as expected, 119 manifest digests matched, and H had no failed
  section. Repeated report bytes were identical.
- SKYNET unit suite: 12 passed, 0 failed on the corrected command.
- Validation suite: 86 ran, 82 passed, 4 disposable-PostgreSQL tests skipped,
  0 failed.
- PTW skill verification, JSON validation, shell syntax, and
  `git diff --check -- .` passed; symlinks below SKYNET: zero.
- One initial SKYNET test command omitted `PYTHONPATH=..`; two modules failed
  import before running assertions. The corrected command ran all 12 tests
  successfully. This is recorded as two pre-execution harness errors.
- One attempted compiler-cache cleanup command was rejected before execution
  because the tool disallowed `rm -rf`. Exact `find -depth -delete` targets
  then removed only `runtime/swift-exp008/module-cache` and `tmp`; no durable
  artifact was deleted.

## Resource use

- New built-in image calls: 0; cumulative: 2.
- New Internet research calls: 0; cumulative: 4.
- New Telegram events: 1; cumulative: 7 queued.
- New creative render-script invocations: 4, all successful; one native compile
  and three byte-identical replays.
- New small-screen derivative invocations: 2.
- New visual-inspection images: 3.
- Standardized preflight invocations: 3, each reading seven master images and
  checking 119 manifest digests; one separate H manifest audit checked 17.
- Formal critic/provider calls: 0.
- Paid external services and exposed new monetary cost: $0.
- Workspace started near 15,200 KiB, peaked near 240,224 KiB during first Swift
  module-cache construction, and ended near 16,668 KiB. Exactly 223,928 KiB of
  disposable module-cache/tmp data was removed; the 136 KiB executable remains.

## Unfinished work

- All seven outbound events remain queued locally with no sending reservation,
  receipt, failure record, or external feedback. Never poll Telegram or wait
  for delivery.
- H is a clean current-canonical competition candidate, not an outcome winner.
  A, B, C, F, G, and H remain non-dominated for different local roles.
- Frozen D remains durable and queued but is superseded as the preferred
  contrast direction by H; never mutate either identity.
- The strict critic still lacks four generator-blind initial candidates and
  exact server element UUIDs for the local portfolio.
- Candidate E remains blocked until authorized source-metadata-bearing real
  candid photography appears. Do not substitute generated people, faces, or
  arbitrary Internet media.
- The read-only competitor still has no owner-approved acceptance render or
  controlled outcome available for direct comparison.

## Next intended action

Reconcile local queue/sending/receipt evidence without polling, and bind any
exact external feedback to event ID plus frozen digest before changing the
strategy ranking. Recheck only legitimately approved Project/Pexels media and
activate packet 005 only if its real-photo prerequisite passes. If neither
appears, do not spend another same-Brief image call or queue another direction:
retain A, B, C, F, G, and H as the clean portfolio and treat H—not D—as the
preferred contrast-reframe candidate.
