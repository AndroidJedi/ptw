# SKYNET checkpoint — human-story fallback integrity

Recorded: 2026-08-31T14:42:11Z

## Where I was

Run 11 restored the complete durable record through run 10: experiments
001–008, seven frozen deliveries and queue events, exact manifests and audits,
the standardized preflight, both clean-portfolio evaluations, the blocked real-
photo packet 005, research, local runtime records, and prior checkpoints.
Reconciliation again found no interrupted creative, sending reservation,
Telegram receipt, failure record, external feedback, authorized Pexels provider,
or approved real Project photo.

Candidate H remains the primary local selection prior. G, A, and B remain its
orthogonal object-, problem-, and offer-led challengers; C and F remain
deliberate-read mechanism controls. Candidate D remains frozen and queued with
its known zero-clearance headline gate. No candidate is an outcome winner.

A current read-only competitor path exposed an apparent contradiction worth
testing: `human_story` is a photo-capable strategy whose local adapter now has a
canonical travertine fallback when no photo is available, while the exact
`human_story@3` generator template, packet-005 owner task, and packet media
policy still require real candid photography.

## What changed

- Used the content-candidate generator contract to resolve the input boundary:
  the adapter fallback does not authorize silently replacing packet 005's real
  candid work scene, so Candidate E was not generated or mutated.
- Added deterministic diagnostic
  `eval-20260831-003-human-story-fallback`, resolving the exact packet-005
  sliders through the current Universal adapter with no photo.
- Added a root-confined replay script, raw 1080 PNG, 360 and 120 PNG
  derivatives, digest-bound render manifest, pixel brand audit, and final
  diagnostic report.
- Inspected the raw and both derivatives under the Studio visual-audit
  discipline. The diagnostic is explicitly marked `not_a_candidate`; it was
  not frozen, added to the preflight portfolio, compared as an outcome, or
  queued to Telegram.
- Added the reusable render-safe-versus-candidate-valid distinction to
  `knowledge/evidence.md` and updated the compact resume state.
- Preserved every existing candidate, queue record, manifest, rank, protected
  copy field, and competitor file. No provider, production service, database,
  campaign, publisher, or external channel was mutated.

## What I learned

- The no-photo fallback is mechanically real: it resolves to deterministic
  travertine texture, needs no background asset, and renders byte-identically.
- It passes both automated geometry layers. Required nodes exist; title,
  supporting line, offer, CTA, and logo remain within safe bounds; text does
  not overflow, truncate, touch clipped boxes, or collide; offer and CTA are
  exact.
- Geometry success is not candidate validity. The result is a sparse editorial
  typography poster with no candid work scene, work environment, human moment,
  or primary subject. The texture does not complete the `human_story@3` visual
  grammar or demonstrate Natal's supplied return mechanism.
- The fallback exposes a hard brand defect missed by geometry: the dark Natal
  wordmark sits directly on a dark field without its light surface. Measured
  contrast is 1.46:1 versus the shared 4.5:1 floor.
- At 360 pixels the headline and CTA remain clear while supporting copy, offer,
  and wordmark recede. At 120 pixels only the headline and CTA are reliable.
- Reusable rule: treat a media fallback as candidate-valid only when the exact
  strategy and task authorize the resulting visual class, CandidateV2 and alt
  text describe those pixels, and brand/message hard gates pass. Renderer
  availability alone is not permission.
- The useful next diagnostic is a four-fallback brand-surface matrix. It can
  determine cheaply whether low Natal wordmark contrast is systematic across
  the current photo-strategy fallbacks without generating another candidate.

## Verification and failures

- Diagnostic raw PNG SHA-256:
  `83373f60aa37692dc03566eb78be49e346ecb9fe4d1ce14dd92782b964619147`.
- Diagnostic render-manifest SHA-256:
  `588ced4b14e43956844ba57e3a3e25658ae9401ad3d45178fb6c0c6c52f9da86`.
- Diagnostic report SHA-256:
  `f4f73e2b4effde5b4020dd2ec2e6e113707d2b8473b09dcf273ef0693384a4d5`.
- The raw render and both derivatives replayed byte-identically across two
  retained comparisons after the final brand audit was added.
- Exact diagnostic Universal layout audit: passed. Exact strict visible-
  geometry audit: passed. Brand-prominence audit: failed as the intended
  experiment result only, at 1.46:1 wordmark contrast.
- General Studio geometry audit: default, high-density, centered-minimal,
  editorial bottom-left, urgent bottom-right, and logo-without-background
  variants passed.
- SKYNET runtime/preflight suite: 12 passed, 0 failed.
- Validation suite: 86 ran, 82 passed, four disposable-PostgreSQL tests skipped,
  0 failed. One earlier invocation was conservatively left recorded as
  untracked because its live session ID was not retained; a second tracked
  invocation completed with exit status 0.
- Canonical PTW skill verification, durable JSON validation, shell syntax,
  symlink check, and `git diff --check -- .` passed.
- Standardized frozen-artifact preflight replay remained byte-identical at
  SHA-256
  `916b230df1e9005bf28515fad4642271511b23066e9d8638c92710fedeb8699f`:
  six candidates passed, frozen D remained the sole expected failure, and all
  119 manifest digests matched.
- One final shell assertion mistyped that known digest and exited nonzero after
  the successful preflight. The corrected exact comparison passed; no report,
  artifact, or code bytes changed.

## Resource use

- New built-in image calls: 0; cumulative: 2.
- New Internet research calls: 0; cumulative: 4.
- New Telegram events: 0; cumulative: 7 queued.
- Diagnostic render-script invocations: 3, all successful; each produced one
  raw render and two small-screen derivatives in a disposable workspace.
- Unique durable diagnostic image files: 3; local visual inspections: 6.
- Candidate-generator, critic, Pexels/provider, publisher, and campaign calls:
  0.
- Validation-suite launches: 2; one exact tracked completion and one
  conservatively counted untracked invocation. SKYNET suite launches: 1.
- Preflight invocations: 1, reading seven master images and checking 119
  manifest digests.
- Paid external services and exposed new monetary cost: USD 0.
- Workspace started near 17,924 KiB and was near 18,412 KiB before this
  checkpoint; growth is primarily the diagnostic render, derivatives, manifest,
  report, checkpoint, evidence, and active runner log.

## Unfinished work

- All seven outbound events remain queued locally with no sending reservation,
  receipt, failure record, or external feedback. Never poll Telegram or wait
  for delivery.
- Packet 005 still requires one authorized source-metadata-bearing real candid
  photo. Never relabel the diagnostic texture render as Candidate E or use
  generated people, faces, or arbitrary Internet media.
- H remains a local action prior, not an outcome winner. Bind any later result
  to exact event ID and frozen digest before changing the portfolio.
- The formal content-result critic still lacks four generator-blind candidates
  and exact server element UUIDs. Never fabricate either.
- The current geometry audit does not enforce pixel brand contrast; SKYNET's
  pixel preflight caught the fallback defect. A future competitor fix is
  outside this isolated writable boundary.
- The browser renderer remains unavailable under the current sandbox; retained
  CoreGraphics renderers remain the safe path for frozen SKYNET candidates.

## Next intended action

Reconcile queue, sending, receipt, exact feedback, and authorized Project/Pexels
media without polling. Activate packet 005 only if its real-photo prerequisite
passes; never use the diagnostic fallback as Candidate E. If no external
evidence appears, run one deterministic, non-candidate matrix over all four
current photo-strategy fallbacks and measure logo/background contrast and
message compatibility. Persist the matrix without another image-model call,
candidate identity, or Telegram event, then decide whether a reusable SKYNET
media-mode/brand gate needs to be added before any future fallback-backed
generation packet.
