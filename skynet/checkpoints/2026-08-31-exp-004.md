# SKYNET checkpoint — exp-20260831-004

Recorded: 2026-08-31T11:21:05Z

## Where I was

The fourth creative cycle is complete. Recovery restored experiments 001-003,
their exact artifacts and checkpoints, three queued outbound events, no local
receipts or external feedback, and the precommitted contrast-reframe packet. No
partial experiment 004, outbound reservation, or interrupted render existed.

The exact new competition candidate is
`experiments/exp-20260831-004/renders/cand-20260831-004-d-v1.jpg` with SHA-256
`6636c1f44ea3edbf4d9ecf4cc912ac710ffc02d42c87bd454352b1a71048e993`.
It is a 1080×1080 JPEG, 99,082 bytes, and replayed byte-identically twice after
freeze.

Outbound event `skynet.exp-20260831-004.g1i1.cand-d` was queued at
2026-08-31T11:13:40Z. Its outbox copy has the same digest. All four competition
events are queued, not confirmed sent; never poll Telegram or wait for delivery.

## What changed

- Consumed packet `packet-exp-20260831-004-d-v1` and preserved the approved
  Brief, exact `90 ДНІВ БЕЗКОШТОВНО` offer, exact `СПРОБУВАТИ NATAL` CTA, named
  Natal assets, strategy values, and zero-image-call reservation.
- Created strict CandidateV2 `cand-20260831-004-d` with the required nine visual
  roles and a deterministic native geometry renderer.
- Rendered a broadcast-versus-scenario contrast: muted message chaos crosses
  into one cyan Risk → reminder → repeat-booking route.
- Persisted exact copy bindings, template snapshot, FFmpeg filter graph, PNG and
  JPEG outputs, manifest, 360-pixel preview, visual audit, and four-way local
  comparison.
- Queued the exact frozen JPEG through the outbound-only outbox.
- Appended experiment evidence, corrected generation-isolation accounting, and
  precommitted blocked packet `packet-exp-20260831-005-e-v1` with exact current
  canonical human-story and Studio digests.

## What I learned

- The compact contrast adds a useful fourth distinction: A is the most concrete
  loss scene, B makes offer/status breadth dominant, C gives the fullest causal
  explanation, and D has the sharpest short reframe. None is an outcome winner.
- A 360-pixel derivative keeps D's headline, Risk state, offer, and CTA legible;
  micro-labels can recede without losing the core message.
- The first successful pre-freeze draft used a right-arrow glyph on a vertical
  connector. Visual inspection caught it; a down arrow fixed directionality
  before the final digest without changing candidate copy, sliders, or geometry
  frames.
- The initial restoration command was too broad for the packet's artifact-
  blindness claim: it opened prior candidate, manifest, and audit JSON before D
  was drafted. Earlier render images remained unseen until D froze, but D is
  neither generator-blind nor artifact-blind. Packet wording cannot undo actual
  context exposure.
- Packet 004's `contrast_reframe` source digest differs from the current
  canonical v3 strategy digest. D remains a valid packet-faithful controlled
  competition artifact, but it is not a verified canonical template replay.
  Future packets must carry exact canonical strategy and Studio snapshot
  digests.
- The unused canonical `human_story@3` strategy requires real candid
  photography. With no approved Project photo or authorized Pexels provider in
  the current tool catalog, generated people or arbitrary Internet photos would
  violate the media boundary. The next packet is honestly blocked instead.

## Verification and failures

- Strict CandidateV2 and exact protected offer/CTA: passed.
- Required nine visual roles in exact order: passed.
- Raw 1080 PNG and 360×360 visual audits: passed with no clipping, collision, or
  unsafe bounds.
- JPEG 1080×1080, SOI/EOI, MIME, digest, and outbox copy: passed.
- All 23 experiment-004 manifest input/output digests: verified.
- Two post-freeze deterministic replays: byte-identical.
- SKYNET runtime tests: 7 passed.
- JSON validation, shell syntax, symlink scan, PTW skill verification, and
  `git diff --check -- .`: passed.
- The host validation suite ran 71 tests: 31 passed, 38 dependency-gated tests
  skipped, and two modules failed import because Pillow and FastAPI are absent.
  The dependency-complete Docker rerun was not repeated; prior durable state
  records that the Docker daemon is unavailable.
- Generation-isolation failure: prior candidate JSON was opened during broad
  recovery. Resolution: D is controlled-only and excluded from formal critic
  eligibility.
- Template-provenance failure: packet source digest did not match the current
  canonical contrast template. Resolution: preserve D, disclose the mismatch,
  and lock exact canonical digests into packet 005.
- Formal critic: intentionally not invoked because the exact five-candidate
  generator-blind boundary is not satisfied.

## Resource use

- New built-in image generation calls: 0; cumulative: 1.
- New Internet searches: 0; cumulative: 0.
- Local render invocations: 4, all successful (initial draft, corrected frozen
  render, two byte-level replays).
- Small-screen preview derivations: 1.
- New Telegram events queued: 1; cumulative: 4.
- Paid external services invoked: 0; exposed new monetary cost: $0.
- Workspace grew from approximately 6,172 KiB at restoration to approximately
  6,916 KiB after candidate, outbox copy, records, and checkpoint.

## Unfinished work

- All four outbound events have queue records and no receipts. Human silence is
  normal and must not block useful work.
- No external controlled result exists, so no strategy is a winner.
- Only candidate A is strict-generator-blind; B, C, and D are controlled
  candidates. The formal critic still lacks four eligible initial candidates.
- Candidate D must never be relabeled artifact-blind or canonical-template
  verified.
- Candidate E is blocked on an authorized, source-metadata-bearing real Pexels
  photo. Do not substitute a synthetic person, generated face, or arbitrary
  Internet download.

## Next intended action

At startup, reconcile local queue and receipt files without polling Telegram.
If exact external feedback appears, bind it to its event and artifact SHA-256
before updating any hypothesis. Otherwise inspect only authorized media/tool
inventory. Activate `plans/exp-20260831-005-generation-packet.json` only if a
real Pexels asset with stable source metadata is available. If it remains
blocked, keep the packet blocked and take one bounded research or design action
that does not weaken media honesty or merely duplicate A-D.
