# SKYNET checkpoint — photo-fallback matrix and activation gate

Recorded: 2026-08-31T15:06:18Z

## Where I was

Run 12 restored the complete durable record through run 11: experiments
001–008, seven frozen deliveries and queue events, exact manifests and audits,
the standardized preflight, the clean-portfolio selection prior, both earlier
glance evaluations, the blocked real-photo packet 005, research, local runtime
records, and prior checkpoints. Reconciliation found no interrupted creative,
sending reservation, Telegram receipt, failure record, external feedback,
authorized Pexels provider, approved Project photo, or local owner feedback.

Candidate H remains the primary local selection prior. G, A, and B remain its
orthogonal object-, problem-, and offer-led challengers; C and F remain
deliberate-read mechanism controls. Candidate D remains frozen and queued with
its known zero-clearance headline gate. No candidate is an outcome winner.

The precommitted next action was a zero-image-call matrix over all four current
photo-strategy fallbacks to determine whether the human-story fallback's 1.46:1
Natal wordmark failure was isolated or systematic.

## What changed

- Added deterministic diagnostic
  `eval-20260831-004-photo-fallback-matrix` for `moment_tension`,
  `contrast_reframe`, `mechanism_proof`, and `human_story` with no photo.
- Held one Brief-grounded content payload constant, used each exact canonical
  v3 default slider set, and rendered four raw 1080 PNGs plus 360- and 120-pixel
  derivatives and two row-major contacts.
- Bound the canonical template, adapter, renderer, audit, Brief, preflight, and
  gate source digests into the replayable render manifest.
- Inspected all four raw masters and both contacts under the Studio visual-audit
  discipline. Every output remains explicitly diagnostic-only and received no
  CandidateV2, candidate identity, portfolio membership, or queue event.
- Added `tools/universal_candidate_gate.py`. It rejects current adapter-v3
  deterministic texture fallbacks before candidate identity and separately
  requires layout, strict visible geometry, and pixel brand prominence to pass.
- Added focused unit coverage proving a texture-backed photo strategy fails
  closed and an audited approved-photo path remains eligible to pass.
- Added the conclusion to `knowledge/evidence.md` and updated compact state.
  No frozen candidate, queue event, competitor file, provider, production
  service, database, campaign, publisher, or external channel was mutated.

## What I learned

- The brand failure is systematic, not human-story-specific. All four dark
  Natal wordmarks are placed directly on dark textured fields without their
  light surface and measure only 1.42–1.47:1 against the 4.5:1 floor.
- Geometry success hides a second systematic failure. The texture names change,
  but all four renders collapse into one dark typography-poster class without
  the primary media subject their exact strategies require.
- `moment_tension` has no candid focal action or observable unresolved moment;
  `contrast_reframe` has no two-state subject or decisive interruption;
  `mechanism_proof` has no visible product/process object or real context; and
  `human_story` has no person, interaction, environment, or human observation.
- At 120 pixels the main headline and yellow CTA retain most of the signal. The
  mechanism, exact offer, bullets where present, and dark wordmark recede or
  disappear. The cyan glyph alone does not provide reliable name attribution.
- A local renderer fallback is an availability affordance, not a creative
  authorization. Future photo strategies need authorized approved photography;
  future typography or non-human graphics need a matching non-photo strategy.

## Verification and failures

- Exact render-manifest SHA-256:
  `50e6a0a6105c388a30598502486c6d2353c4d3a05d35dccf33eabddf321af298`.
- Exact evaluation-report SHA-256:
  `3ec1b77666534bb56355f3e7eea56bc56d08dacc4e7ba4f1801be1e0efe62728`.
- 360 contact SHA-256:
  `9eacc59f1dc67108d6edc6d32f0daf448080b337454341ff9847aed5715d4ffc`.
- 120 stress contact SHA-256:
  `4c0cb6837e9fcf4f0dd4c25be22fa4384dea3677272b62fe0ea0eb70c20fa412`.
- The final manifest and complete 14-image output set replayed byte-identically
  across two consecutive matrix runs.
- Universal layout audits: four passed, zero failed. Strict visible-geometry
  audits: four passed, zero failed. Brand audits: zero passed, four failed as
  the experiment result. Activation gates: zero passed, four failed with exact
  codes `deterministic_texture_diagnostic_only` and
  `brand_prominence_failed`.
- All 14 SKYNET runtime/preflight tests passed, including two new activation-
  gate cases. The Studio geometry regression passed default, high-density,
  centered-minimal, editorial bottom-left, urgent bottom-right, and
  logo-without-background variants.
- One reconciliation command included a zsh glob for a nonexistent local-owner
  records directory and stopped with `no matches found`; a glob-safe rerun
  confirmed the store contains only its empty schema record. No state changed.
- Python compilation, durable JSON parsing, root symlink inspection, and
  `git diff --check -- .` passed at final verification.

## Resource use

- New built-in image calls: 0; cumulative: 2.
- New Internet research calls: 0; cumulative: 4.
- New Telegram events: 0; cumulative: 7 queued.
- Matrix-script runs: 3, all successful; 12 local raw render invocations, 24
  small-preview derivations, and six contact derivations. Fourteen unique
  diagnostic image files remain durable.
- Local visual inspections: six images—four raw 1080 masters, one 360 contact,
  and one pixel-preserving 120 stress contact.
- Candidate activation decisions: four final checks; all failed closed.
- Candidate-generator, critic, Pexels/provider, publisher, campaign, sender,
  and poller calls: 0.
- Paid external services and exposed new monetary cost: USD 0.
- Workspace started near 18,660 KiB, peaked near 23,888 KiB while six disposable
  Studio regression renders were retained, and returned near 19,752 KiB after
  those files and a compile cache were removed.

## Unfinished work

- All seven outbound events remain queued locally with no sending reservation,
  receipt, failure record, or external feedback. Never poll Telegram or wait
  for delivery.
- Packet 005 still requires one authorized source-metadata-bearing real candid
  photo. Never relabel any deterministic texture as Candidate E or use
  generated people, faces, or arbitrary Internet media.
- H remains a local action prior, not an outcome winner. Bind any later result
  to its exact event ID and frozen digest before changing the portfolio.
- The formal content-result critic still lacks four generator-blind candidates
  and exact server element UUIDs. Never fabricate either.
- Frozen D remains historically valid but outside the clean set. Do not mutate
  its bytes or queued identity.
- The browser renderer remains unavailable in this sandbox; retained
  CoreGraphics sources/executables remain the safe frozen-candidate replay path.

## Next intended action

On the next fresh run, reconcile only newly available queue reservation,
receipt, failure, exact feedback, authorized Project/Pexels media, or a
materially new approved Brief—without polling. If exact feedback appears, bind
it before changing H's selection prior. If authorized photography appears,
activate packet 005 and apply the new activation gate. Otherwise preserve H and
the clean portfolio: do not rerun this matrix, generate another same-Brief
direction, or queue routine progress. A new creative experiment requires new
external evidence, authorized media, or a materially different approved Brief.
